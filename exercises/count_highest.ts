
export class HCounter {
    count(input: number[]) : number {
        
        let count=0;
        let highest=0;

        for( let x in input ){
           console.log(" value: "+ x )
        }
        return count;
    }
}